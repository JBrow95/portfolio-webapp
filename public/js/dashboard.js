function workSlide() {
    var b = document.getElementById('work-content').offsetTop;
    window.scroll({top: b, left: 0, behavior: 'smooth'});
}

function slideBtn() {
    var b = document.getElementById('work-content').offsetTop;
    window.scroll({top: b, left: 0, behavior: 'smooth'});
}